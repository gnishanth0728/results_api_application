# configure the database connection
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/school
    username: student
    password: student123

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
